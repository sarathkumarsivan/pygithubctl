sudo: false
language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
install:
  - pip install -e .
  - pip install -U pip setuptools wheel
  - pip install -r requirements.txt
script:
  - pytest -v